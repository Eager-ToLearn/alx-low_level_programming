#include "main.h"
/**
 *  _abs - computes the absolute value of an integer
 *@m: input number as an integer
 * Return: Always 0 (Success)
 */

int _abs(int m)

{
if (m >= 0)
{
	return (m);
}
else
{
	return (m * -1);

}
}
